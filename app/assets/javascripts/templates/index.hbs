{{#each properties}}
  <h3><span class="label label-default">Featured Property</span></h3>
  <div class="row">
    <div class="col-sm-7">
      <div class="img-feature-div">
        <img {{bind-attr src="this.featurePhotoUrl"}} class="img-responsive" />
      </div>
    </div>

    <div class="col-sm-5">
      <h2 class="featurette-heading">{{this.name}}</h2>
      <p class="lead">{{this.description}}</p>
      <address>
        <strong>Address</strong><br>
        {{this.address}}<br>
        {{this.city}}, {{this.state}} {{this.zip}}<br>
      </address>

      <address>
        <strong>Contact Info</strong><br>
        Cody Tanner <br>
        (801) 637-4393
      </address>
    </div>

  </div>
{{/each}}